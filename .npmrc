# pnpm configuration for NymAI deployment

# Skip optional dependencies (prevents canvas native build failure on DigitalOcean)
optional=false

# Allow non-frozen lockfile for DO build compatibility
frozen-lockfile=false
