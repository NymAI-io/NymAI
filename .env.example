# NymAI Environment Variables Example
# Copy this file to .env and fill in your actual values
# NEVER commit .env files to version control

# =============================================================================
# DATABASE CONFIGURATION (Supabase)
# =============================================================================
# Get this from Supabase Project Settings > Database > Connection String
# Format: postgresql://postgres:[YOUR-PASSWORD]@[PROJECT-REF].supabase.co:5432/postgres
SUPABASE_DB_URL=

# Get this from Supabase Project Settings > API > service_role key
# Used for server-side operations
# NOTE: Below is secret key (service_role key is legacy)
SUPABASE_SECRET_KEY=

# Get this from Supabase Project Settings > API > Project URL
SUPABASE_URL=

# Get this from Supabase Project Settings > API > anon/public key
# NOTE: Below is publishable key (anon key is legacy)
SUPABASE_PUBLISHABLE_KEY=

# =============================================================================
# ZENDESK OAUTH CONFIGURATION
# =============================================================================
# Create OAuth client in Zendesk Admin Center > Apps and integrations > APIs > OAuth Clients
ZENDESK_CLIENT_ID=
ZENDESK_CLIENT_SECRET=

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================
# Environment: development | staging | production
NODE_ENV=development

# API Base URL (used by frontend/admin console)
# Local: http://localhost:3000
# Production: https://api.nymai.com
API_URL=http://localhost:3000

# Admin Console URL
# Local: http://localhost:5173
# Production: https://app.nymai.com
ADMIN_URL=http://localhost:5173

# =============================================================================
# VITE CONFIGURATION (Admin Console)
# =============================================================================
# These are used by the Vite-based admin console
# Note: Vite requires VITE_ prefix for client-side env vars
VITE_API_URL=http://localhost:3000
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# =============================================================================
# MCP SERVER CONFIGURATION
# =============================================================================
# These are used by workspace-scoped MCP servers in .mcp.json

# DigitalOcean API Token (Primary deployment platform)
# Get from: https://cloud.digitalocean.com/account/api/tokens
# Required scopes: read, write
DIGITALOCEAN_ACCESS_TOKEN=

# Render API Key (Alternative deployment platform)
# Get from: https://dashboard.render.com/settings#api-keys
RENDER_API_KEY=

# Zendesk Configuration
# Email address for Zendesk API authentication
ZENDESK_EMAIL=

# Zendesk API Token
# Get from: Zendesk Admin Center > Apps and integrations > APIs > Zendesk API > Settings
ZENDESK_API_TOKEN=